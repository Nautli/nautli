# nautli 차체 UX 스펙 v1 (2026-07-12 밤샘 빌드)

> 목표: 신규 유저가 마케팅 없이 제품만으로 "설치→AI 연결→습관 심기→데몬→아침 카드"를 완주.
> 진입점 = 로컬 대시보드. 마케팅(랜딩·영상) 스코프 제외.

## 유저 스토리 (완주 시나리오)
1. `git clone … && npm install && npm link` (배포 전) / 배포 후 `npm i -g nautli`
2. `nautli dashboard` → 브라우저 http://127.0.0.1:4600 자동 오픈
3. Setup 탭(첫 방문 자동): 체크리스트 5개를 버튼으로 완료
4. 다음날 아침: Review 탭 배지 → 카드 O/X 클릭
5. 평소: AI가 remember/recall (유저 무개입), 가끔 Memory 탭에서 검색

## 화면 1: Setup (미완료 항목 있으면 기본 탭)
체크리스트 항목 = 상태아이콘(✓완료/○미완/!오류) + 제목 + 설명 1줄 + 액션 버튼:
| # | 항목 | 상태 판정 | 버튼 → 동작 |
|---|---|---|---|
| 1 | 저장소 초기화 | `~/.nautli/index.sqlite` 존재 | [초기화] → POST /api/setup/init |
| 2 | Claude Code 연결 | `claude` CLI 존재 + `claude mcp list`에 nautli | [자동 등록] → claude mcp add 실행. 실패 시 수동 명령 코드블록+[복사] |
| 3 | AI 습관 지시문 | `~/.claude/CLAUDE.md`에 마커(`<!-- nautli:instructions -->`) 존재 | [CLAUDE.md에 설치] / [지시문 복사] |
| 4 | 밤 소화 데몬 (03:30) | `launchctl list`에 com.bug.nautli-daemon(추후 com.nautli.daemon) | [데몬 설치] → plist 생성+bootstrap / [지금 1회 실행] → daemon-run 백그라운드+결과 표시 |
| 5 | 다른 AI 도구 연결 (옵션) | — | [Cursor 설정 복사] (mcp.json 스니펫) |
| 6 | 기존 노트 이식 (옵션) | — | "준비 중" 배지 (v0.2) — 정직 표기 |
- 모든 버튼: 실행 중 스피너 → 결과 토스트(성공 초록/실패는 이유 문장) → 상태 재조회
- 전부 ✓면 상단에 "설치 완료 — 이제 AI가 알아서 기억합니다" 배너

## 화면 2: Review (아침 카드) — SPEC §9 카드 규칙 준수
- 데이터: `~/.nautli/review/queue.jsonl` status=pending
- 카드 구성(위→아래): 유형 배지(중복 정리|모순 발견) + 판정 confidence + **사람언어 한 줄**("이 두 기억이 같은 내용 같아요. 하나로 합칠까요?") + 기억 A/B 원문 (각각 scope·subject·t_valid 병기) + judge 근거(reason, 접힘)
- 버튼(카드 유형별):
  - 중복: [합치기] [따로 유지] [모름/나중에]
  - 모순: [새 기억이 맞음] [옛 기억이 맞음] [둘 다 유효함] [기타…(텍스트 입력)]
- **위험색 idle 금지**: 모든 버튼 평상시 중립색, hover/선택 시만 강조색
- 처리(비파괴, non-lossy):
  - 중복 합치기 = 오래된 쪽 transition(superseded) / 따로 유지 = 큐 항목 status:"dismissed"(기억 무변경) / 모름 = status:"deferred"(내일 다시)
  - 모순: 새것맞음 = 옛것 invalidated / 옛것맞음 = 새것 invalidated / 둘다유효 = 무변경+dismissed
  - **기타 텍스트 = 정정 루프**: 입력 문장을 remember(scope 동일, confidence 0.9, source:"review-card")로 새 fact 저장 + 큐 항목 status:"answered"
- 빈 상태: "검토할 카드가 없어요. 다음 소화는 오늘 새벽 3:30." / 에러 상태: 파일 깨짐 시 원인+doctor 안내
- 오라클 라우팅(v0 표기): 카드 하단에 "이 판단이 레포/코드로 확인 가능한 내용이면 '모름'을 눌러도 됩니다 — 다음 소화 때 자동 재판정" 힌트 문구

## 화면 3: Memory (기억 브라우저)
- 상단: 검색창(FTS) + scope 드롭다운(전체/개인/프로젝트별) + [죽은 기억 포함] 토글(기본 off=active만)
- 리스트 행: claim + 배지(scope, t_valid, confidence, status — superseded/invalidated는 회색+취소선 아님, 도장 아이콘)
- 행 클릭 → 상세 패널: 전체 필드 + 이력(대체됨→by 링크)
- 상단 [기억 추가] 입력창: 문장+scope 선택 → remember (게이트 거부 시 이유 토스트 — E_DUPLICATE 등 사람말 번역)

## 화면 4: Stats
- 카드 4개: 총 기억 수 / 상태 분포(active·superseded·invalidated) / 스코프별 top / 마지막 소화(시각+적용·큐 수)
- 리포트 목록(reports/*.md) → 클릭 뷰어(렌더된 md)

## 공통
- 네비: Setup / Review(＋pending 배지) / Memory / Stats. 헤더 우측: 데몬 헬스 점(초록=24h 내 exit0)
- 브랜드 표기 = src/brand.js BRAND 상수 (개명 시 1곳)
- 서버: `nautli dashboard [--port 4600] [--no-open]` — node 내장 http, **127.0.0.1 고정 바인딩**, 외부 의존성 0, 단일 HTML(인라인 CSS/JS)+/api/* JSON
- 보안: POST만 상태 변경, Origin 헤더가 127.0.0.1이 아니면 403, 셸 실행은 화이트리스트 커맨드만(claude mcp add/launchctl/…), 토큰·시크릿 로그 금지
- CLI 미러: `nautli setup`(대화형 5단계, 대시보드와 동일 로직 공유), `nautli review`(터미널 O/X), doctor 보강(node 버전·claude CLI·데몬 상태)

## v1.1 확정 변경 (sol UX 크리틱 → 디렉터 수용/기각, 2026-07-12)
**수용:**
1. **첫날 가치 경험(최중요)**: Setup 마지막 단계 = "체험 소화" — 샘플 기억 2쌍(source:"sample", 삭제 가능) 저장 → 즉시 소화 1회 → 샘플 카드가 Review에 뜸 → 첫 O/X를 설치 5분 안에 경험. 완료 후 [샘플 지우기] 버튼
2. Setup을 **필수 4개**(저장소/Claude연결/지시문/데몬)+선택 섹션으로 분리 — 완료 배너는 필수만 판정. "노트 이식 준비중" 항목 삭제
3. 데몬 신뢰: 상태 판정 = plist 존재+최근 health.log exit=0 조합. 마지막 실행 24h+ 경과 시 경고+[지금 소화 테스트]. "다음 실행" 시각을 정확 계산해 표시. Setup에서 1회 실행 성공이 완료 조건
4. CLAUDE.md 수정 전 **추가될 블록 diff 미리보기 모달** + 마커 기반이라 [지시문 제거] 역방향 제공. 데몬도 uninstall 커맨드 제공
5. 동시성 최소 방어: 카드 처리는 pair_id 기반 멱등(이미 non-pending이면 no-op), queue 재작성은 tmp+atomic rename
6. Origin allowlist 명확화: `http://127.0.0.1:<port>`, `http://localhost:<port>` exact만 허용, 상태 변경은 POST만
7. 카피 교체: [기억 저장소 만들기] / [Claude Code 지시문 추가] / [지금 소화 테스트] / [내일 다시 보기] / 완료 배너 "연결 완료 — 대화 중 필요한 기억을 저장할 수 있어요"
8. 오라클 재판정 힌트 문구 삭제(미구현 기능 거짓 기대 금지)
**기각(사유):** Stats 전체 삭제(→카드 4개로 축소 유지: 데몬 신뢰 표면이라 필요) / Memory 수동 추가 삭제(→유지: 정정 루프와 동일 코드 경로+게이트 거부 데모 가치) / CSRF 토큰(→v0는 로컬 단일유저+Origin 체크로 충분, v0.2)
**리포트 md 뷰어 삭제 수용** — Stats에 마지막 소화 요약 숫자만.

## v1.2 확정 변경 (2026-07-12 신규유저 UX 점검 — 유저 지시 "기억 추가 제거 + 아주 깔끔하게")
1. **Memory 수동 [기억 추가] 폼 삭제** (v1.1 기각을 유저 지시로 뒤집음). 정정 루프는 Review 카드의 기타 입력([정정 저장])만 남김. `/api/memory` POST와 CLI remember는 유지(API 계약 불변).
2. **Stats 탭 삭제** → Memory 상단 요약줄("기억 N개 · 지난 기억 M개 · 스코프 K개 · 마지막 소화 시각")로 흡수. 데몬 신뢰 표면은 헤더 헬스 점+dawn 진행바+Review 하단 컨텍스트가 담당.
3. **Setup = 안내형 스텝퍼**: 가치 헤드라인("모든 AI가 공유하는 하나의 뇌")+진행도 N/4, 미완 첫 스텝만 primary(주황) 버튼·강조 테두리, 완료 스텝은 ✓ 압축 행(보조 동작은 quiet 버튼), 선택 섹션(체험 소화·Cursor)은 필수 4개 완료 후에만 노출. 완료 시 배너+체험 소화 히어로(★).
4. **빈 상태 3종 안내형**: Review 빈+설정미완→"먼저 설정 완료"+이동 버튼 / 설정완료+샘플전→체험 소화 버튼 / Memory 저장소 자체가 빈 경우→"Claude와 대화하면 자동으로 쌓여요"(검색 탓 카피 제거).
5. **용어 사람말화**: 탭 설정/카드/기억, "죽은 기억 포함"→"지난 기억 포함", scope 배지 개인/절차/프로젝트 X, 카드 confidence→"확신 N%", subject 없으면 배지 생략, 헤더 데몬 상태는 plist 설치 전 숨김(설치 전 "데몬 미설치" 부정신호 제거), "밤 소화 정상" 표기.
6. 외부 카피 줄표(—) 금지 준수(배너 "연결 완료 · 이제 AI가 알아서 기억해요").
- 검증: 테스트 42/42 유지, 격리 홈 신규유저/완료상태 2서버 실주행(카드 처리·기타 폼·검색·요약줄), 모바일 375px 스윕. 헤드리스 --window-size 캡처는 뷰포트 미에뮬레이션으로 우측 잘림 아티팩트가 나므로 모바일 검안은 인앱 브라우저 실측 기준.

## v1.3 확정 변경 (2026-07-13 건강검진 온보딩 — 유저 지시 "진단→비효율 리포트→처방" 퍼널, 무료 카피는 제외)
1. **스텝 0 = 내 기억 건강검진** (`src/onboard/checkup.js` + vendor/vault-doctor): 후보 자동 탐지(`.obsidian` 볼트 + `~/.claude` 하네스, 홈 하위만·depth 3) → 시작 모달(후보 라디오+직접 입력+데이터 경계 고지: "판정 텍스트만 내 Claude 구독 경유 Anthropic 처리") → 맛보기 진단(노트 40개 캡·judge쌍 150·junk표본 12, **실측 30노트≈8분**) → 리포트 카드.
2. **리포트 카드** = 점수/100 + 사람언어 헤드라인("같은 주제가 서로 다르게 적혀 있어요. AI가 어느 쪽을 믿을지 복불복") + 중복·모순·junk 수치 + 대표 부딪히는 기록 실물 2건 + 처방 CTA: [이 기억 N건 가져오고 연결 계속](primary) / [전체 리포트] / [가져오지 않고 새로 시작].
3. **가져오기 = 진짜 얹기**: 진단 atoms를 remember 게이트 경유로 저장(source:"checkup", procedural→procedure, 중복은 게이트가 거름, 캡 800) — 이후 밤 소화가 **유저 자신의 볼트에서 나온 모순**을 카드로 만든다(합성 샘플 대체, NA-005·007 응답).
4. 검진이 리드인 동안(none/running/done/failed) 필수연결 primary·next 강조는 강등(주황 1개 원칙). dismissed면 선택 섹션에 재진입 행. imported면 ✓ 압축 행.
5. **코어 수정**: validScope가 유니코드 문자 허용 — `project:백로그` 같은 한글 스코프가 MCP remember·임포트에서 거부되던 잠재 버그(W1 한국 파일럿 치명) 해소.
6. 보안: 진단 경로는 홈 하위만+NAUTLI_HOME 제외, spawn은 argv 배열(셸 0), 동시 1런.
- 검증: 테스트 48/48(gate 한글 스코프·checkup 4케이스 신규), 대시보드 실주행 e2e(후보 감지→시작→python 스폰→진행 폴링→[스테이징] 리포트 카드→가져오기 3건+중복 거름→기억 탭 실물), 소형 볼트 실 스폰 왕복.

## 성공 기준 (착수 시 앵커 — 채점 루브릭)
1. 격리 홈(NAUTLI_HOME) 신규유저 e2e: setup→remember→daemon-run→카드 발생→대시보드에서 O/X→저장소 반영까지 실측 왕복 (30점)
2. Setup 5항목 버튼이 실제 동작+실패 시 사람말 사유 (20점)
3. 카드 규칙 준수: 사람언어 번역·출처날짜·위험색 idle 금지·정정 루프 (20점)
4. 기존 테스트 21/21 유지 + 신규 로직 테스트 추가 (10점)
5. 비주얼 QA: 빈/정상/에러 상태 매트릭스 스크린샷 결함 0 (10점)
6. 보안: 127.0.0.1 고정·Origin 체크·화이트리스트 셸 (10점)

## v1.4 확정 변경 (2026-07-13 밤 — P0-3/4/5, 정본=docs/uiux/p03-p05-final-spec.md)
1. **연속성 스텝 "첫 진짜 기억"**: Setup 필수 4개 완료 후 히어로. 스텝A(유효 기억 붙여넣기 1회+activity 폴링 감지+대기카피 3단계) → 스텝B(대시보드가 recall 직접 시연, 앱 전환 0) → 스텝C(Cursor 연결자 한정 크로스툴 검증). 건너뛰기=회색 "건너뜀"(거짓 완료 금지). 중복 claim도 연결 증명으로 처리("이미 있던 기억이에요").
2. **공유 카드**: 발견 카운트 프레임(점수는 하단 소형 배지+표본 라벨), canvas 로컬 PNG, 원문·경로 필드 자체 부재(테스트 고정), X 스니펫 복사. 캔버스 폰트는 system-ui 선두(-apple-system은 ctx.font 파서가 거부→세리프 폴백, 비주얼 QA로 적발).
3. **검진 하드닝**: claude 로그인 사전검사+복구 버튼, 데이터 경계 정직 카피, 최상위 폴더 체크박스 제외, 예상 파일수·시간, 부분 결과 스트리밍+첫 발견 티저, 시드 셔플(재현성).
4. **코어**: recall 이벤트 로그(append-only, rebuild 왕복 무손상) + GET /api/activity + 전 요청 Host 검증(DNS rebinding 차단).
- 검증: 테스트 60/60, 연속성 격리 홈 e2e(모의 MCP), 공유 카드 4상태 비주얼 QA(0건/큰수/일반/만점 — 겹침·잘림 0, 폰트 FAIL 1건 수정 후 재검안 PASS). 리뷰 2라운드(sonnet 8.0+codex 3.5→메타 판정→블로커 6건 수정→라운드2 5 FIXED+1 PARTIAL→회귀 픽스).
