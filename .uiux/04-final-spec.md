디렉터 합성 스텝(`director-synthesis`)이 실행됐지만 **선행 3개 입력이 없어서 종합할 대상이 없다.** 이대로 진행하면 없는 걸 지어내는 것.

## 확인된 상태
- `/Users/bugbookee/Desktop/buildkit/.uiux/` 안에 `01-proposal.md`·`02-expert-critique.md`·`03-user-feedback.md` **모두 없음**. 있는 건 옛날(2026-06-10) `04-final-spec.md` 하나 — 그때 "매거진 제작 보드" 스펙.
- 이번 프롬프트엔 `SERVICE`/`TARGET_USER`/`INTENT` 같은 컨텍스트도 안 실려 옴 (템플릿 `{{PLACEHOLDER}}` 미치환).
- 이 상태에서 4단계(합성)만 호출된 형태.

## 원인 후보
1. 파이프라인 JSON에서 앞 3스텝을 건너뛰고 `director-synthesis`만 실행. 이러면 `input:[...]` 배열의 파일이 존재하지 않아 프롬프트에 아무것도 안 붙음.
2. 파이프라인 JSON은 정상인데 앞 3스텝 output이 다른 경로로 나갔거나 삭제됨.
3. 템플릿을 복사만 하고 `{{PLACEHOLDER}}`를 실제 값으로 치환 안 한 채로 실행 → 앞 스텝들이 "무엇을 설계할지" 모른 채 돌았거나 실패.

## 필요한 것 (택1)
- **A. 이 세션에서 무엇을 설계하려는 건지 알려줘** (서비스/타겟유저/의도) → 3스텝 다시 돌리자. `templates/uiux-design-debate.template.json`을 실제 파일로 복사→플레이스홀더 채움→`node buildkit.js run <file>.json`.
- **B. 앞 스텝들이 이미 돌았고 산출물이 다른 경로에 있으면** 그 경로 알려줘. 붙여서 합성 진행.
- **C. 이건 옛 매거진 보드 스펙 재합성이면** 그 의도를 명시해줘 (그때 `01`~`03`이 없으니 기존 `04`를 시드로 재토론해야 하는데, 이건 표준 절차 밖).

임의로 매거진 보드 스펙을 재출간하지 않는다 — 지금 무엇을 만드는지 확정 안 됐고, 3자 토론이 실제로 안 일어난 상태로 "합성"이라 보고하면 CLAUDE.md의 "실행 안 한 걸 완료로 보고 금지" 위반.
